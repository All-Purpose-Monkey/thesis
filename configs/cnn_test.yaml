seed: 42

ssl: #ssl not used here just kept around for naming consistency in the run file
  epochs: 200
  batch_size: 512
  lr: 0.05

downstream:
  epochs: 100
  batch_size: 256
  lr: 0.0001
  weight_decay: 0.001

data:
  test_size: 0.2

appliances:
  - kettle
  - toaster
  - microwave
  - dishwasher
  - fridge
  - washing_machine